{% extends "base.tmpl" %}

{% block title %}List Incoming Pointers{% endblock %}

{% block main %}

<section class="pointerlist">

{% set pointer_base = "$artemis-pointers.dev" %}

{% for ptr in pointers %}
  <div class="card pointer">
    <h3 class="card-header">
      {{pointer_base}}/{{ptr.in}}
    </h3>
    <div class="card-body">
      <p class="card-text pointer-type">
        <strong>Type:</strong> {% if ptr.type=="XRPL_ADDRESS" %}Direct Payout{% elif ptr.type=="PAYMENT_POINTER" %}Interledger Relay{% elif ptr.type=="RIPPLENET" %}RippleNet Relay{% else %}(Unknown){% endif %}
      </p>
      <p class="card-text pointer-dest">
        <strong>To:</strong> <span class="{{ptr.type|lower}}">{{ptr.to}}</span>
      </p>
      <a href="#" class="btn btn-primary">View status</a>
      <a href="#" class="btn btn-secondary">Edit</a>
      <a href="#" class="btn btn-danger">Delete</a>
    </div>
  </div>
{% endfor %}

</section>

{% endblock %}

{% block left_sidebar %}
<ul>
  <li>Lorem</li>
  <li>ipsum</li>
  <li>dolor</li>
  <li>sit</li>
  <li>amet</li>
</ul>
{% endblock %}
